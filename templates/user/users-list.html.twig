{% extends 'base.html.twig' %}

{% block body %}
    {{ form(filters) }}
    <h1>Users</h1>
    <ul>
        {% for user in users %}
            <li>{{ user.email }} {{ user.username }} <a href="{{ path('users_edit', {id: user.id}) }}">Edit</a></li>
        {% endfor %}
    </ul>
{% endblock %}
